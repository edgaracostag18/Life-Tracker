package com.grit.liferecorder.core.model

enum class LabelType { Positive, Negative, Neutral }
enum class TaskPriority { Low, Medium, High }
enum class TaskStatus { Open, Completed, Canceled }
enum class RoutineStatus { Scheduled, Completed, Skipped, Missed }
enum class CalendarDotType { Task, PositiveAction, NegativeAction, NeutralAction, Routine, Journal }
